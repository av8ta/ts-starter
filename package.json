{
  "version": "1.0.1",
  "private": "true",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "npm run build && node --test tests",
    "cli": "NODE_OPTIONS='--loader ts-node/esm' ./bin/index.ts"
  },
  "volta": {
    "node": "18.8.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.13",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  }
}